#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age = 40;
    double gpa = 3.6;
    char grade = 'D';
    char phrase[] = "Death Academy"
    //we can use these type of variable, we also have a variable call float but it's a bit different.

    return 0;
}
